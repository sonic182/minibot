[pytest]
asyncio_mode = strict
testpaths = tests
python_files = tests/test_*.py
norecursedirs = llm-async aiosonic
addopts = --cov=minibot --cov-report=term --cov-report=html --cov-fail-under=0
markers =
    e2e: End-to-end tests that require external services.
