# Example prompts

- Use `$python-hex-review` to review this diff for layer violations between `minibot.app` and `minibot.adapters`.
- Use `$python-hex-review` to refactor this handler so provider-specific logic moves out of `minibot.app.handlers`.
- Use `$python-hex-review` to check whether this new helper belongs in `minibot.shared` or a narrower package.
