services:
  minibot:
    build: .
    container_name: minibot
    restart: unless-stopped
    init: true
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./logs:/app/logs
      - ./data:/app/data
    command: ["minibot"]
